# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Secrets (API keys) — never commit
.env

# Downloaded data (re-creatable by running the pipeline)
data/raw/*.parquet
data/processed/*.parquet

# Keep the empty folders in git
!data/raw/.gitkeep
!data/processed/.gitkeep
