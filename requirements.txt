# Python dependencies for the European Power Fair Value project.
# Install with:  pip install -r requirements.txt

requests>=2.31      # calling the Energy-Charts REST API
pandas>=2.0         # data wrangling
pyarrow>=14.0       # reading/writing Parquet files
matplotlib>=3.7     # QA figures
tabulate>=0.9       # rendering DataFrames as Markdown tables in the QA report
scikit-learn>=1.3   # baselines + gradient boosting model (Task 2)
holidays>=0.40      # German public-holiday calendar feature (Task 2)
anthropic>=0.40     # LLM client for the AI commentary component (Task 4)
jupyter>=1.0        # running the notebook
